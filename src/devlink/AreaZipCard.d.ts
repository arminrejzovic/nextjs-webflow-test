import * as React from "react";
import * as Types from "./types";

declare function AreaZipCard(props: {
  as?: React.ElementType;
  longName?: React.ReactNode;
  zip?: React.ReactNode;
}): React.JSX.Element;
